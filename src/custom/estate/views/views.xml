<?xml version="1.0"?>
<odoo>
  <data>
    <record id="estate_property_view_form" model="ir.ui.view">
      <field name="name">estate.property.form</field>
      <field name="model">estate.property</field>
      <field name="arch" type="xml">
        <form string="Properties">
          <header>
            <button name="action_set_status_sold" type="object" string="Sold" attrs="{'invisible': [('state', '!=', 'offer accepted')]}"/>
            <button name="action_set_status_canceled" type="object" string="Cancel" attrs="{'invisible': [('state', '=', 'sold')]}"/>
            <field name="state" widget="statusbar" statusbar_visible="new,offer received,offer accepted,sold"/>
          </header>
          <sheet>
            <h2 class="mt16 mb16">
              <field name="name"/>
            </h2>
            <group>
              <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
            </group>
            <group>
              <group>
                <field name="state" string="Status"/>
                <field name="property_type_id" string="Property Type" options="{'quick_create': False}"/>
                <field name="postcode"/>
                <field name="date_availability" String="Available From"/>
              </group>
              <group>
                <field name="expected_price" String="Expected Price"/>
                <field name="best_price" String="Best Offer"/>
                <field name="selling_price" String="Selling Price"/>
              </group>
            </group>
            <notebook>
              <page string="Description">
                <group>
                  <field name="description"/>
                  <field name="bedrooms"/>
                  <field name="living_area" string="Living Area (sqm)"/>
                  <field name="facades"/>
                  <field name="garage"/>
                  <field name="garden"/>
                  <field name="garden_area" string="Garden Area (sqm)" attrs="{'invisible': [('garden', '=', False)]}"/>
                  <field name="garden_orientation" attrs="{'invisible': [('garden', '=', False)]}"/>
                  <field name="total_area" string="Total Area (sqm)"/>
                </group>
              </page>
              <page string="Offers">
                <field name="offer_ids" attrs="{'readonly': [('state', 'in', ('offer accepted', 'sold', 'canceled'))]}"/>
              </page>
              <page string="Other Info">
                <group>
                  <field name="salesperson"/>
                  <field name="buyer"/>
                </group>
              </page>
            </notebook>
          </sheet>
        </form>
      </field>
    </record>

    <record id="estate_property_view_tree" model="ir.ui.view">
      <field name="name">estate.property.tree</field>
      <field name="model">estate.property</field>
      <field name="arch" type="xml">
        <tree
          default_order="name desc"
          decoration-success="state=='offer accepted'"
          decoration-bf="state=='offer accepted'"
          decoration-muted="state=='sold'">
          <field name="name"/>
          <field name="postcode"/>
          <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
          <field name="bedrooms"/>
          <field name="living_area" string="Living Area (sqm)"/>
          <field name="expected_price" string="Expected Price"/>
          <field name="selling_price" string="Selling Price"/>
          <field name="date_availability" string="Available From" optional="hide"/>
          <field name="state" string="Status"/>
        </tree>
      </field>
    </record>

    <record id="estate_property_view_search" model="ir.ui.view">
      <field name="name">estate.property.search</field>
      <field name="model">estate.property</field>
      <field name="arch" type="xml">
        <search string="Properties">
          <field name="name" string="Title"/>
          <field name="postcode"/>
          <field name="expected_price"/>
          <field name="bedrooms"/>
          <field name="living_area" string="Living Area (sqm)" filter_domain="[('living_area', '>=', self)]"/>
          <field name="facades"/>
          <filter string="Available" name="state" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer received')]"/>
          <group expand="1" string="Group By">
            <filter name="postcode" context="{'group_by':'postcode'}"/>
          </group>
        </search>
      </field>
    </record>
  </data>
</odoo>